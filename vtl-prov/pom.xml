<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>fr.insee.trevas</groupId>
        <artifactId>trevas-parent</artifactId>
        <version>2.6.0-SNAPSHOT</version>
    </parent>

    <name>VTL Provenance</name>
    <description>Provenance static analysis</description>

    <artifactId>vtl-prov</artifactId>
    <version>2.6.0-SNAPSHOT</version>

    <properties>
        <sonar.coverage.jacoco.xmlReportPaths>
            ${project.basedir}/../coverage/target/site/jacoco-aggregate/jacoco.xml
        </sonar.coverage.jacoco.xmlReportPaths>
        <!-- Test-only Spark bridge: same public package fr.insee.vtl.spark.* in both artifacts.
             Default Spark 4: vtl-sdmx (test) and fusion-sdmx-vtl need ANTLR 4.13.x on the classpath. -->
        <trevas.spark.integration.test.module>vtl-spark4</trevas.spark.integration.test.module>
        <trevas.spark.processing.engine>spark4</trevas.spark.processing.engine>
        <antlr.runtime.version>${antlr4.spark4.version}</antlr.runtime.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.jgrapht</groupId>
            <artifactId>jgrapht-core</artifactId>
        </dependency>
        <dependency>
            <groupId>fr.insee.trevas</groupId>
            <artifactId>vtl-parser</artifactId>
        </dependency>
        <dependency>
            <groupId>fr.insee.trevas</groupId>
            <artifactId>vtl-engine</artifactId>
        </dependency>
        <dependency>
            <groupId>fr.insee.trevas</groupId>
            <artifactId>vtl-model</artifactId>
        </dependency>
        <dependency>
            <groupId>fr.insee.trevas</groupId>
            <artifactId>vtl-sdmx</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Jena 5.x requires Java 17+; Jena 6.x requires Java 21. -->
        <dependency>
            <groupId>org.apache.jena</groupId>
            <artifactId>apache-jena-libs</artifactId>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- Spark is not a compile dependency: applications add either vtl-spark or vtl-spark4.
             Tests pick one module via trevas.spark.integration.test.module (profile spark3-tests). -->
        <dependency>
            <groupId>fr.insee.trevas</groupId>
            <artifactId>${trevas.spark.integration.test.module}</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Pin ANTLR for SDMX + Spark 4; do not rely on transitive mediation across test deps. -->
        <dependency>
            <groupId>org.antlr</groupId>
            <artifactId>antlr4-runtime</artifactId>
            <version>${antlr.runtime.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>spark3-tests</id>
            <properties>
                <trevas.spark.integration.test.module>vtl-spark</trevas.spark.integration.test.module>
                <trevas.spark.processing.engine>spark</trevas.spark.processing.engine>
                <jackson-bom.version>${jackson-bom.spark3.version}</jackson-bom.version>
                <antlr.runtime.version>${antlr4.version}</antlr.runtime.version>
            </properties>
        </profile>
        <!-- Alias kept for CI matrix entries that still pass -Pspark4-tests. -->
        <profile>
            <id>spark4-tests</id>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>--add-exports java.base/sun.nio.ch=ALL-UNNAMED</argLine>
                    <environmentVariables>
                        <SPARK_USER>test</SPARK_USER>
                        <HADOOP_USER_NAME>test</HADOOP_USER_NAME>
                    </environmentVariables>
                    <systemPropertyVariables>
                        <trevas.spark.processing.engine>${trevas.spark.processing.engine}</trevas.spark.processing.engine>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
