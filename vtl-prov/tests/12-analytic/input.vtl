// $input ds1: id STRING IDENTIFIER, t INTEGER IDENTIFIER, m1 INTEGER MEASURE
// Trevas grammar: OVER sits inside the analytic call parens (not after them).
ds2 := ds1[calc running := sum(m1 over (partition by id order by t))];
